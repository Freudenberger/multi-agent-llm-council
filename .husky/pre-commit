#!/usr/bin/env sh
# Fast pre-commit gate. See docs/test-plan.md §6.
# Only lint-staged runs here: ESLint --fix on staged .ts/.tsx files.
# The heavier whole-project gates (typecheck, full lint) run on pre-push.
npx lint-staged
